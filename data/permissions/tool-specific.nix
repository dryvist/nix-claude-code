_:
# Per-tool permission overrides. Tools (claude, codex, gemini, copilot)
# consume `lib.permissions` + their own entry here.
{
  claude = {
    allow = [ ];
    ask = [ ];
    deny = [ ];
  };
  codex = {
    allow = [ ];
    ask = [ ];
    deny = [ ];
  };
  gemini = {
    allow = [ ];
    ask = [ ];
    deny = [ ];
  };
}
